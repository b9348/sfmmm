# 云数据库配置（复制此文件为 .env 后填入真实值）
DB_URL=mysql://user:password@host:port/database

# 数据库字段加密密钥（64 位十六进制 = 32 字节，AES-256-GCM；生成: openssl rand -hex 32）
# 更换该密钥会导致已加密数据无法解密，请与构建/CI 环境保持一致
DB_ENC_KEY=your_64_hex_encryption_key

# 图床配置
IMGBED_URL=https://your-imgbed-host.example.com
IMGBED_TOKEN=your_imgbed_token_here
